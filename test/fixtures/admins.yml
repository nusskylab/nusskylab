# fixtures for admin for cohort 2018
<% (2..5).each do |n| %>
admin_<%= n %>:
  id: <%= n %>
  user_id: <%= n %>
  created_at: <%= 0.days.from_now.to_s :db %>
  updated_at: <%= 0.days.from_now.to_s :db %>
  cohort: 2018
<% end %>

# fixtures for admin for cohort 2019
<% (1702..1705).each do |n| %>
admin_<%= n %>:
  id: <%= n %>
  user_id: <%= n %>
  created_at: <%= 0.days.from_now.to_s :db %>
  updated_at: <%= 0.days.from_now.to_s :db %>
  cohort: 2019
<% end %>
