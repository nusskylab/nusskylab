<% content_for :main_content do %>
  <% javascript 'tutors.js' %>
  <div class="panel panel-info">
    <div class="panel-heading">
      <h3 class="text-center">All Tutors in cohort <%= cohort %></h3>
    </div>
    <div class="panel-body">
      <div class="col-sm-12">
        <div class="col-sm-2 col-sm-offset-5">
          <a href="<%= new_tutor_path(cohort: cohort) %>" class="btn btn-success">Create New Tutor</a>
        </div>
      </div>
      <br><br><hr>
      <div class="col-sm-12">
        <% all_cohorts.each do |cohort| %>
        <div class="col-sm-2">
          <%= link_to cohort, tutors_path(cohort: cohort), class: 'btn btn-info' %>
        </div>
        <% end %>
      </div>
      <br><br><hr> 
      <%= render 'tutors_table', locals: {tutors: @roles} %>
      <hr>
      <div class="col-sm-12">
        <% all_cohorts.each do |cohort| %>
          <div class="col-sm-2">
            <%= link_to cohort, tutors_path(cohort: cohort), class: 'btn btn-info' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
