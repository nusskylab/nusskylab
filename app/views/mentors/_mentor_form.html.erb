<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="text-center"><%= locals[:is_new] ? 'Create' : 'Edit' %> mentor</h3>
  </div>
  <div class="panel-body">
    <%= form_for @mentor, :url => {action: (locals[:is_new] ? 'create' : 'update')}, :html => {:class => 'form-horizontal'} do |f| %>
        <div class="form-group">
          <label class="col-sm-2 control-label" for="user-nus-id">NUS ID:</label>
          <div class="col-sm-10">
            <input class="form-control" type="text" placeholder="Input user's NUS ID"
                   name="nus_id" id="user-nus-id" value="<%= locals[:is_new] ? '' : @mentor.user.uid[26..-1] %>">
          </div>
        </div>
        <div class="form-group">
          <label class="col-sm-2 control-label" for="user-username">Username:</label>
          <div class="col-sm-10">
            <input class="form-control" type="text" placeholder="Input user name"
                   name="user_name" id="user-username" value="<%= locals[:is_new] ? '' : @mentor.user.user_name %>">
          </div>
        </div>
        <div class="form-group">
          <label class="col-sm-2 control-label" for="user-user-email">User email:</label>
          <div class="col-sm-10">
            <input class="form-control" placeholder="Input user email"
                   name="user_email" id="user-user-email" value="<%= locals[:is_new] ? '' : @mentor.user.email %>" %>
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <%= f.submit 'Submit', class: 'btn btn-success' %>
          </div>
        </div>
    <% end %>
  </div>
</div>
