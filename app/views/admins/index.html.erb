<% content_for :main_content do %>
  <% javascript 'admins.js' %>
  <div class="row">
    <h3 class="col text-right">All Admins in cohort <%= cohort %></h3>
    <div class="col text-right">
      <%= link_to 'Create New Admin', new_admin_path(cohort: cohort), class: 'btn btn-success' %>
    </div>
  </div>
  <div class="row">
    <%= render 'admins_table', locals: {admins: @roles} %>
  </div>
  <ul class="nav col-sm-12">
    <% all_cohorts.each do |cohort| %>
      <li class="nav-item col-sm-2">
        <%= link_to cohort, admins_path(cohort: cohort), class: 'btn btn-info' %>
      </li>
    <% end %>
  </ul>
<% end %>
